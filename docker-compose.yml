services:
  rag:
    build: .
    ports:
      - "8000:8000"
    environment:
      LLM_BACKEND: ${LLM_BACKEND:-extractive}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      TOP_K: ${TOP_K:-4}
      MAX_DISTANCE: ${MAX_DISTANCE:-1.05}
